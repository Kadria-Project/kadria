'use client'
import { hasPermission } from '@/src/lib/team/permission-matrix'
import { PermissionGate } from '@/src/components/settings/PermissionGate'
import { ReadOnlyNotice } from '@/src/components/settings/ReadOnlyNotice'
import { useIntegrationSettingsData } from '@/src/hooks/useIntegrationSettingsData'
export function IntegrationSettingsView() { const data = useIntegrationSettingsData(); const canManage = hasPermission(data.role ?? 'viewer', 'integrations.manage'); if (data.loading) return <div className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">Chargement des connexions…</div>; return <PermissionGate role={data.role} permission="integrations.read" fallback={<ReadOnlyNotice message="Vous n’avez pas accès aux connexions." />}><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-4"><div><h2 className="text-base font-semibold text-slate-900">Google Calendar</h2><p className="mt-1 text-sm text-slate-600">Lecture des événements, création des rendez-vous et synchronisation de l’agenda.</p></div><span className={data.calendar.connected ? 'rounded-full bg-emerald-50 px-3 py-1 text-xs font-medium text-emerald-700' : 'rounded-full bg-slate-100 px-3 py-1 text-xs font-medium text-slate-600'}>{data.calendar.connected ? 'Connecté' : 'Non connecté'}</span></div>{data.calendar.connected && <dl className="mt-4 grid gap-3 text-sm sm:grid-cols-2"><div><dt className="text-slate-500">Compte</dt><dd className="font-medium text-slate-800">{data.calendar.email || 'Non communiqué'}</dd></div><div><dt className="text-slate-500">Connexion</dt><dd className="font-medium text-slate-800">{data.calendar.connectedAt ? new Date(data.calendar.connectedAt).toLocaleDateString('fr-FR') : 'Non disponible'}</dd></div></dl>}{!canManage && <ReadOnlyNotice message="La gestion des connexions est réservée au propriétaire et aux administrateurs." />}{canManage && <div className="mt-5 flex flex-wrap gap-2"><button onClick={data.connect} className="rounded-lg bg-emerald-600 px-4 py-2 text-sm font-medium text-white">{data.calendar.connected ? 'Reconnecter' : 'Connecter Google Calendar'}</button>{data.calendar.connected && <button onClick={() => void data.disconnect()} disabled={data.action === 'disconnecting'} className="rounded-lg border border-slate-300 px-4 py-2 text-sm font-medium text-slate-700">{data.action === 'disconnecting' ? 'Déconnexion…' : 'Déconnecter'}</button>}</div>}{data.error && <p role="alert" className="mt-3 text-sm text-red-600">{data.error}</p>}</section></PermissionGate> }
