'use client'
import{useAccessSettingsData}from '@/src/hooks/useAccessSettingsData';export function AccessSettingsView(){const d=useAccessSettingsData();if(d.loading)return <div className="rounded-xl border border-slate-200 bg-white p-5">Chargement…</div>;return <div className="space-y-4"><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-semibold text-slate-900">Identité de connexion</h2><p className="mt-2 text-sm text-slate-600">{d.email||'Adresse non disponible'}</p><p className="mt-2 text-sm text-slate-600">Méthode active : lien magique envoyé par e-mail.</p></section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-semibold text-slate-900">Connexion sécurisée</h2><p className="mt-2 text-sm text-slate-600">Aucun mot de passe ni liste de sessions n’est exposé par les contrats actuels.</p><button onClick={()=>void d.magicLink()} disabled={!d.email||d.action==='magic'} className="mt-4 rounded-lg bg-emerald-600 px-4 py-2 text-sm font-medium text-white">{d.action==='magic'?'Envoi…':'Envoyer un lien de connexion'}</button></section><section className="rounded-xl border border-red-200 bg-white p-5 shadow-sm"><h2 className="font-semibold text-slate-900">Actions de sécurité</h2><button onClick={()=>void d.logout()} disabled={d.action==='logout'} className="mt-4 rounded-lg border border-red-300 px-4 py-2 text-sm font-medium text-red-700">{d.action==='logout'?'Déconnexion…':'Se déconnecter'}</button></section>{d.message&&<p className="text-sm text-slate-600">{d.message}</p>}</div>}
