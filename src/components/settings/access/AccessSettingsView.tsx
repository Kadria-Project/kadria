'use client'

import { FormEvent, useState } from 'react'
import { useAccessSettingsData } from '@/src/hooks/useAccessSettingsData'

export function AccessSettingsView() {
  const data = useAccessSettingsData()
  const [editingPassword, setEditingPassword] = useState(false)
  const [password, setPassword] = useState('')
  const [confirmation, setConfirmation] = useState('')
  if (data.loading) return <div className="rounded-xl border border-slate-200 bg-white p-5">Chargement…</div>
  async function submitPassword(event: FormEvent<HTMLFormElement>) {
    event.preventDefault()
    if (password !== confirmation) return
    await data.savePassword(password)
    setPassword(''); setConfirmation(''); setEditingPassword(false)
  }
  return <div className="space-y-4"><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-semibold text-slate-900">Identité de connexion</h2><p className="mt-2 text-sm text-slate-600">{data.email || 'Adresse non disponible'}</p><p className="mt-2 text-sm text-slate-600">Vous pouvez vous connecter avec un mot de passe ou un lien magique.</p></section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-semibold text-slate-900">Mot de passe</h2><p className="mt-2 text-sm text-slate-600">{data.passwordConfigured ? 'Mot de passe configuré' : 'Aucun mot de passe défini'}</p>{editingPassword ? <form onSubmit={submitPassword} className="mt-4 max-w-md space-y-3"><input className="w-full rounded-lg border border-slate-300 px-3 py-2 text-sm" type="password" autoComplete="new-password" placeholder="Nouveau mot de passe (12 caractères minimum)" value={password} onChange={(event) => setPassword(event.target.value)} required minLength={12} /><input className="w-full rounded-lg border border-slate-300 px-3 py-2 text-sm" type="password" autoComplete="new-password" placeholder="Confirmer le mot de passe" value={confirmation} onChange={(event) => setConfirmation(event.target.value)} required /><div className="flex gap-3"><button disabled={data.action === 'password' || password !== confirmation} className="rounded-lg bg-emerald-600 px-4 py-2 text-sm font-medium text-white disabled:bg-slate-400">{data.action === 'password' ? 'Enregistrement…' : 'Enregistrer'}</button><button type="button" onClick={() => setEditingPassword(false)} className="text-sm text-slate-600">Annuler</button></div>{password && confirmation && password !== confirmation && <p className="text-sm text-red-600">Les deux mots de passe ne correspondent pas.</p>}</form> : <button onClick={() => setEditingPassword(true)} className="mt-4 rounded-lg bg-emerald-600 px-4 py-2 text-sm font-medium text-white">{data.passwordConfigured ? 'Modifier le mot de passe' : 'Créer un mot de passe'}</button>}</section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-semibold text-slate-900">Lien magique</h2><p className="mt-2 text-sm text-slate-600">Recevez un lien de connexion si vous préférez cette méthode.</p><button onClick={() => void data.magicLink()} disabled={!data.email || data.action === 'magic'} className="mt-4 rounded-lg border border-emerald-600 px-4 py-2 text-sm font-medium text-emerald-700">{data.action === 'magic' ? 'Envoi…' : 'Recevoir un lien magique'}</button></section><section className="rounded-xl border border-red-200 bg-white p-5 shadow-sm"><h2 className="font-semibold text-slate-900">Actions de sécurité</h2><button onClick={() => void data.logout()} disabled={data.action === 'logout'} className="mt-4 rounded-lg border border-red-300 px-4 py-2 text-sm font-medium text-red-700">{data.action === 'logout' ? 'Déconnexion…' : 'Se déconnecter'}</button></section>{data.message && <p className="text-sm text-slate-600" role="status">{data.message}</p>}</div>
}
