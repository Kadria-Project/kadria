import { BriefcaseBusiness, Clock3, FileCheck2, MapPin } from 'lucide-react';
import type { NormalizedCalendarEvent } from '@/src/lib/calendar/normalized-event';
import { eventLabel, formatTime } from './calendar-workspace-utils';

export default function NextAppointmentPanel({ event, onOpenProject }: { event: NormalizedCalendarEvent | null; onOpenProject: (event: NormalizedCalendarEvent) => void }) {
  return <section id="workspace-section-next-appointment" className="rounded-2xl border border-slate-200 bg-white p-4"><p className="flex items-center gap-2 text-[10px] font-bold uppercase tracking-[0.14em] text-slate-500"><BriefcaseBusiness className="size-4 text-blue-600" />Préparation prochain rdv</p>{event ? <div className="mt-3"><div className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-xl bg-blue-50 text-blue-600"><BriefcaseBusiness className="size-4" /></span><div><p className="text-sm font-semibold text-slate-900">{eventLabel(event)}</p><p className="mt-0.5 text-xs text-slate-500">{event.projectTitle || event.title}</p></div></div><div className="mt-3 space-y-1.5 text-xs text-slate-600"><p className="flex items-center gap-2"><Clock3 className="size-3.5 text-slate-400" />{formatTime(event.start) || 'Horaire à préciser'}</p>{(event.address || event.location) && <p className="flex items-center gap-2"><MapPin className="size-3.5 text-slate-400" />{event.address || event.location}</p>}{event.status && <p className="flex items-center gap-2"><FileCheck2 className="size-3.5 text-slate-400" />{event.status}</p>}</div>{event.actionUrl && <button type="button" onClick={() => onOpenProject(event)} className="mt-4 rounded-lg border border-slate-200 px-3 py-1.5 text-xs font-semibold text-slate-700 hover:bg-slate-50">Voir le dossier</button>}</div> : <p className="mt-3 text-sm leading-6 text-slate-500">Aucun rendez-vous à préparer.</p>}</section>;
}
