import { AlertTriangle, CalendarClock, CircleCheck, MapPinned, UserRound } from 'lucide-react';
import type { ScheduleSituation } from '@/src/lib/calendar/schedule-situations';

type Props = { situations: ScheduleSituation[]; onAction: (situation: ScheduleSituation) => void };

const iconFor = (kind: ScheduleSituation['kind']) => kind === 'assign' ? UserRound : kind === 'travel_margin' ? MapPinned : kind === 'confirm' ? CalendarClock : AlertTriangle;

export default function AgendaSituationsPanel({ situations, onAction }: Props) {
  if (!situations.length) return <section id="workspace-section-decisions" className="rounded-2xl border border-emerald-100 bg-emerald-50/60 px-5 py-4"><div className="flex items-start gap-3"><span className="grid size-9 shrink-0 place-items-center rounded-xl bg-white text-emerald-700"><CircleCheck className="size-5" /></span><div><p className="text-sm font-bold text-emerald-950">Aucun engagement à sécuriser pour le moment.</p><p className="mt-1 text-sm text-emerald-800">Je continue de surveiller les confirmations, affectations et changements.</p></div></div></section>;
  return <section id="workspace-section-decisions" aria-labelledby="agenda-situations-title"><div className="flex items-end justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-slate-500">Avant de partir</p><h2 id="agenda-situations-title" className="mt-1 text-lg font-bold tracking-tight text-slate-950">Engagements à sécuriser</h2><p className="mt-1 text-sm text-slate-600">Voici uniquement ce qui peut compromettre votre journée.</p></div><span className="shrink-0 rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-700">Maximum 3</span></div><div className="mt-3 space-y-3">{situations.map((situation) => { const Icon = iconFor(situation.kind); return <article key={situation.id} id={`workspace-action-${situation.id}`} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-[0_1px_2px_rgba(15,23,42,0.03)]"><div className="flex items-start gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-amber-50 text-amber-700"><Icon className="size-5" /></span><div className="min-w-0"><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-emerald-700">Engagement à sécuriser</p><h3 className="mt-1 text-base font-bold text-slate-950">{situation.clientName || situation.projectTitle || 'Intervention à préparer'}</h3>{situation.projectTitle && situation.clientName ? <p className="mt-0.5 text-sm text-slate-500">{situation.projectTitle}</p> : null}</div></div><div className="mt-4 grid gap-3 text-sm leading-6"><p><span className="block text-[10px] font-bold uppercase tracking-[0.12em] text-slate-500">Ce que Kadria a observé</span>{situation.observedFacts.join(' ')}</p><p><span className="block text-[10px] font-bold uppercase tracking-[0.12em] text-slate-500">Ce que cela signifie</span>{situation.understanding}</p><p><span className="block text-[10px] font-bold uppercase tracking-[0.12em] text-slate-500">Pourquoi cela compte</span>{situation.importance}</p>{situation.consequence ? <p><span className="block text-[10px] font-bold uppercase tracking-[0.12em] text-slate-500">Ce qui peut arriver</span>{situation.consequence}</p> : null}<p className="font-medium text-slate-900"><span className="block text-[10px] font-bold uppercase tracking-[0.12em] text-slate-500">Je vous recommande</span>{situation.recommendation}</p></div>{situation.missingInformation?.length ? <p className="mt-3 text-xs text-amber-800">À compléter : {situation.missingInformation.join(', ')}.</p> : null}<div className="mt-4 flex flex-wrap items-center gap-3"><button type="button" onClick={() => onAction(situation)} className="rounded-lg bg-emerald-500 px-4 py-2.5 text-sm font-bold text-emerald-950 transition-colors hover:bg-emerald-400 focus-visible:outline focus-visible:outline-2 focus-visible:outline-emerald-600 focus-visible:outline-offset-2">{situation.primaryAction?.label || 'Préparer l’intervention'}</button><span className="text-xs text-slate-500">Confiance {situation.confidence === 'high' ? 'élevée' : situation.confidence === 'medium' ? 'moyenne' : 'limitée'}</span></div></article>})}</div></section>;
}
